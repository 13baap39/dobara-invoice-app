# Environment Variables for Dobara Invoice App

# JWT Secret Key (REQUIRED)
# Generate a strong, random secret key for JWT token signing
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here

# Frontend API Configuration
VITE_API_BASE_URL=http://localhost:5002

# MongoDB Connection (if needed in future)
# MONGODB_URI=mongodb://localhost:27017/dobara

# Server Configuration
# PORT=5002
